import styled from 'styled-components';
import { Col } from '../../../utils/GlobalStyles';
import { TypeHeader3 } from '../../atoms/Typography';

export const FormCol = styled(Col)`
  width: 30rem;
`;

export const Header = styled(TypeHeader3)`
  margin: 0;
`;
