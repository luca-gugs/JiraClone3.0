import React from 'react';
import { BaseHoverButton, BaseSubmitButton } from './styles';

export const HoverButton = props => {
  return <BaseHoverButton {...props} />;
};

export const SubmitButton = props => {
  return <BaseSubmitButton {...props} />;
};
